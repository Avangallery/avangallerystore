{
  "name": "avan-gallery-no-r2",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute avan_gallery --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute avan_gallery --remote --file=./schema.sql"
  },
  "devDependencies": {"wrangler": "^4.0.0"}
}